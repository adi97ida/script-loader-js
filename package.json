{
  "name": "script-stylesheet-loader",
  "version": "1.0.0",
  "description": "A library for loading JavaScript and CSS files asynchronously",
  "license": "Private",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/"
  ],
  "scripts": {
    "prebuild": "del lib /Q",
    "build": "tsc --outDir lib --project tsconfig-build.json && esbuild lib/index.js --minify --outfile=lib/script-loader.min.js"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.19.2",
    "typescript": "^5.2.2"
  }
}
